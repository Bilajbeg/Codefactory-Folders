<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    {% if is_granted("ROLE_ADMIN") %}
                    <a class="nav-link active" aria-current="page" href="{{ path('app_product_index') }}">Home</a> {% elseif is_granted("ROLE_USER") %}
                    <a class="nav-link active" aria-current="page" href="/user/">Home</a> {% else %}

                    <a class="nav-link active" aria-current="page" href="/">Home</a> {% endif %}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Link</a>
                </li>

            </ul>

        </div>
    </div>
</nav>